# typed: strict

class <%= class_name %> < T::Struct
  extend T::Sig
  include OpenapiSorbetRails::Interface::Component

  const :value, <%= type %>

  delegate :as_json, to: :value
end

<%= spec_doc.gsub(/^/, "# ").strip %>
